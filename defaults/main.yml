---
# kibana version to install
kibana_version: "4.5"

# debian / ubuntu apt variables
apt_key_url: https://packages.elastic.co/GPG-KEY-elasticsearch
apt_repo: "deb http://packages.elastic.co/kibana/{{ kibana_version }}/debian stable main"

# allow supplying nodejs options
node_options: "--max-old-space-size=250"

# kibana config
kibana_port: 5601
kibana_host: "0.0.0.0"
elasticsearch_url: "http://localhost:9200"
kibana_index: ".kibana"